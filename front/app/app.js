'use strict';


(function() {

    // Declare app level module which depends on views, and components
    angular.module('raquetes', [
        'ui.bootstrap',
        'restangular',
        'ui.router',
        'ui.bootstrap'

    ]);

})();
